/**
 * Next.js configuration.
 * Intentionally minimal — edits here are documentation-only unless exports change.
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

export default nextConfig;
